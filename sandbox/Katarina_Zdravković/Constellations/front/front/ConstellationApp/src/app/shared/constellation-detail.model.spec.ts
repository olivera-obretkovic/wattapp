import { ConstellationDetail } from './constellation-detail.model';

describe('ConstellationDetail', () => {
  it('should create an instance', () => {
    expect(new ConstellationDetail()).toBeTruthy();
  });
});
